@using BuisnessLogic.models
@{
    ViewData["Title"] = "Производители";
}

<div class="text-center">
    <h1 class="display-4">Все производители</h1>
</div>
@if (ViewBag.Producers == null)
{
    <div class="text-center" id="error-div">
        <h3 style="color: red">Ошибка отображения информации</h3>
    </div>
}
else
{
    <div class="column-md-18">
        <br>
        @if (ViewBag.Producers.Count == 0)
        {
            <div class="text-center" id="error-div">
                <h4>Производителей нет</h4>
            </div>
        }
        <br>
        <form method="get" asp-controller="Admin" asp-action="ProducerCreate">
            <button type="submit" style="width: 20%; margin-left: 40%" class="btn-primary">
                Добавить производителя
            </button>
        </form>
        <br>
        @foreach (var elem in (ViewBag.Producers as List<Producer>))
        {
            <div class="column-md-16" style="border-style: solid; border-width: 2px; align-content: center; width: 50%; margin: 0 auto">
                <div class="text-center" style="font-size: 18px">
                    <a asp-controller="Admin" asp-action="Producer" asp-route-id=@elem.Id>Производитель номер @elem.Id</a>
                </div>
                <div class="text-center">
                    Название: @elem.Name
                </div>
            </div>
            <br>
            <br>
        }
    </div>
}